
# Install all required packages
pip install langchain langchain-openai langgraph pydantic python-dotenv

# If you have issues, try upgrading pip first
python -m pip install --upgrade pip
pip install langchain langchain-openai langgraph pydantic python-dotenv
